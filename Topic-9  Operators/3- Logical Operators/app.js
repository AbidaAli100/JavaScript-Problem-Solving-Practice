

/*What happens if we write:


!(!(4>2)) */

true && true; // both are true, so the aswer will be true

false && false; // both are false, so the answer will be true

false || true; // one among them is true, so the answer will be true

5 > 4 && 2 < 5; // both are true, so the answer will be true

2 == 9 || 2==2; // one statement is true, the answer will be true

!(!(4>2)); //  !4>2 is false but !(!(4>2)) is true





